const header = document.getElementById('header');
const description = document.getElementById('description');

header.classList.add('animated', 'fadeIn');
description.classList.add('animated', 'fadeIn');